{
  // These tasks will run in order when initializing your CodeSandbox project.
  "setupTasks": [
    {
      "name": "pip install --upgrade setuptools",
      "command": "pip install --upgrade setuptools"
    },
    {
      "name": "pip install numpy==1.21.0",
      "command": "pip3 install wheel==0.36.2"
    },
    {
      "name": "pip3 install wheel==0.36.2",
      "command": "pip3 install numpy==1.26.0b1"
    },
    {
      "name": "pip install -r requirements.txt",
      "command": "pip3 install -r requirements.txt"
    },
    {
      "command": "pip3 install tensorflow",
      "name": "pip install tensorflow"
    }
  ],

  // These tasks can be run from CodeSandbox. Running one will open a log in the app.
  "tasks": {
    "start": {
      "name": "start",
      "command": "python main.py",
      "runAtStart": true
    },
    "python app.py": {
      "name": "python app.py",
      "command": "python app.py",
      "runAtStart": true
    }
  }
}
